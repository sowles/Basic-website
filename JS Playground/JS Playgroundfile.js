let matrix1 = new Array(3, 4, 2,
                        1, 3, 3,
                        3, 3, 3);

let matrix2 = new Array(3, 4, 2,
                        1, 3, 3,
                        3, 3, 3);

function addmatrix (matrix1, matrix2){
  let matrixFinal = new Array();

  for (item in matrix1) {
    solution += matrix1[item] + matrix2[item];
  }

  return matrixSolution;
}
